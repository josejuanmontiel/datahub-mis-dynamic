source:
  type: my_source.mis_source.DynamicSource
  config:
  # place for your custom config defined in the configModel
    env: DEV
    absolute: "true"
    rootPath: /home/jose/workspace/datahubio/single_mce.json
sink:
  type: "console"
